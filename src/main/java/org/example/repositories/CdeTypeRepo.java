package org.example.repositories;

import org.example.entities.CdeType;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CdeTypeRepo extends JpaRepository<CdeType, Long> {
}
